#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(void) {

    int cases;
    cin >> cases;

    while(cases--) {
        int rows, cols;
        cin >> rows >> cols;
        
        while(rows--) {
           string str = "";
           getline(cin, str);
        }
    }

    return 0;
}
